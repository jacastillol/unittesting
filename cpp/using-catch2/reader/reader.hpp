#include <iostream>
#include <vector>


class Reader
{
    std::vector<std::string> split(const std::string& line, const char delimiter)
    {
        std::vector<std::string> result;
        return result;
    }
};